"""
Shared pytest fixtures.

Adjust the `create_app` import if app.py doesn't yet use an app-factory
pattern — see the note in wsgi.py for the same assumption.
"""
import os
import sys

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

import pytest


@pytest.fixture
def app():
    os.environ["FLASK_ENV"] = "testing"
    from app import create_app

    app = create_app()
    app.config.update(TESTING=True)
    yield app


@pytest.fixture
def client(app):
    return app.test_client()
